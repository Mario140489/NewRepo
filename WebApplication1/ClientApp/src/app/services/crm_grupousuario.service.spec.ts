/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Crm_grupousuarioService } from './crm_grupousuario.service';

describe('Service: Crm_grupousuario', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Crm_grupousuarioService]
    });
  });

  it('should ...', inject([Crm_grupousuarioService], (service: Crm_grupousuarioService) => {
    expect(service).toBeTruthy();
  }));
});
